// This is a stand-alone JavaScript file. It can be run in node.js.
// At the terminal prompt, type: node numbers.js. 
// Note: you must be in the subdirectory where the JavaScript file is located.

let studentScore = 18;
let maxScore = 20;
let percent = 'Your score is ' + (studentScore / maxScore) * 100 + '%';

console.log(percent);
